<!DOCTYPE html>
<html>
    <head>
        <title>postIT!</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <script defer src="/script.js"></script>
        <link href="/common.css" rel="stylesheet" type="text/css" />
        <link href="/style.css" rel="stylesheet" type="text/css" />
        <!-- This is the default, so no exact link is needed.-->
    </head>
    <body>
        <div id="navbarBackground">
            <img src="./_img/list.svg" alt="loading" id="navbarLines" />
            <div id="topnav">
                <a class="navA" href="/create">New Post</a>
                <% if (signedIn) { %>
                <a class="navA" href="/user/<%=userId%>">Your Posts</a>
                <% } else { %>
                <a class="navA" href="/signin">Signin</a>
                <% } %>
                <a class="navA" href="/tos">ToS</a>
            </div>
        </div>
        <main>
            <h1 id="title">PostIT</h1>
            <p>Got an idea, problem or just bored? PostIT then tf?</p>
            <h1>Discover</h1>
            <discover>
                <div id="devPosts">
                    <div>Dev Posts</div>
                    <% devPosts.forEach((post) => {%>
                    <post>
                        <name><%= post.title %></name>
                        <content><%= post.content %></content>
                    </post>
                    <% }) %>
                </div>
                <div id="newPosts">
                    <div>New Posts</div>
                </div>
                <div id="sponsoredPosts">
                    <div>Sponsored Posts</div>
                </div>
                <div id="archivedPosts">
                    <div>Archived</div>
                </div>
            </discover>
        </main>
    </body>
</html>
